datapull:
  api:
    s3_bucket_name:
  secretstore:
    vault:
      vault_url:
      static_secret_path_prefix:
      vault_nonce:
      vault_path_login:
  json:
    inputjson: Input_Sample_teradata-to-filesystem.json
  spark:
    network: spark.network.timeout
    timeout: 10000s
    executor: spark.executor.heartbeatInterval
    interval: 1000s
    broadcasttimeout: spark.sql.broadcastTimeout
    btimeout: 36000
    scheduler: spark.scheduler.mode
    mode: FAIR
    failures: spark.task.maxFailuers
  miscellaneous:
    no_of_retries: 16
    timeout: 1000000ms
    consul_url:
    sleeptimeout: 60000
  application:
    region:
  logger:
    s3:
      loggingfolder: /Users/nishah/Documents/Github/datapull/core/
    mssql:
      server:
      database:
      login:
      password:
      table:
      driver: com.microsoft.sqlserver.jdbc.SQLServerDriver
    cloudwatch:
      groupName: /ecs/datatools-datapull-api
      streamName: datapull-stream
  email:
    smtp:
      emailaddress:
      smtpserveraddress:
    ses:
      email:
      access_key:
      secret_key: